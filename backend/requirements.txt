# SevaSetu Backend – Python dependencies
# Pin major versions; let minor/patch float for security patches.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9        # required for file uploads and form data

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0,<3.0.0
aiosqlite>=0.20.0              # async SQLite driver
alembic>=1.13.0,<2.0.0        # database migrations

# ── Configuration & validation ────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
email-validator>=2.1.0         # required by pydantic EmailStr

# ── Security / Auth ───────────────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0            # JSON Web Tokens
passlib[bcrypt]>=1.7.4,<2.0.0  # password hashing
bcrypt>=4.0.1,<5.0.0           # pin to avoid passlib deprecation warning

# ── HTTP client (used by rule_service for external API calls) ─────────────────
httpx>=0.27.0

# ── Machine learning (rule rejection risk model) ──────────────────────────────
scikit-learn>=1.4.0,<2.0.0
numpy>=1.26.0,<3.0.0
sentence-transformers>=3.0.0  # RAG embeddings (uses cached model, no API needed)

# ── AI / LLM ──────────────────────────────────────────────────────────────────
boto3>=1.34.0                  # AWS SDK for Bedrock LLM API

# ── PDF generation (requires native GTK libs on the server) ───────────────────
jinja2>=3.1.0
weasyprint>=62.0

# ── Document OCR ──────────────────────────────────────────────────────────────
easyocr>=1.7.0

# ── MIME detection ────────────────────────────────────────────────────────────
python-magic>=0.4.27         # requires libmagic native lib

# ── File encryption ───────────────────────────────────────────────────────────
cryptography>=42.0.0

# ── AI / RAG pipeline ────────────────────────────────────────────────────────
langdetect>=1.0.9              # language detection for multilingual support

# ── Speech (TTS) ──────────────────────────────────────────────────────────────
gTTS>=2.5.0                    # text-to-speech synthesis

