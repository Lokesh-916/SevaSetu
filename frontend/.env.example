# URL of the FastAPI backend
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_NAME=SevaSetu

# NOTE: The Groq API key lives ONLY in backend/.env
# There is no VITE_GROQ_API_KEY — all AI calls go through the backend.
